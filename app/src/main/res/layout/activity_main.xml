<?xml version="1.0" encoding="UTF-8"?>

<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <com.esri.arcgisruntime.mapping.view.MapView
        android:id="@+id/mapView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"/>

    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/layers_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginEnd="@dimen/margin_standard"
        android:layout_marginTop="@dimen/margin_standard"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:backgroundTint="@color/gray_200"
        android:clickable="true"
        android:contentDescription="@null"
        android:focusable="true"
        android:tint="@color/gray_800"
        app:rippleColor="@color/gray_800"
        app:srcCompat="@drawable/layers"
        />

    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/topo_style_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintEnd_toEndOf="@+id/layers_button"
        app:layout_constraintTop_toBottomOf="@+id/layers_button"
        android:layout_marginTop="@dimen/margin_standard"
        android:backgroundTint="@color/gray_200"
        android:clickable="true"
        android:contentDescription="@null"
        android:focusable="true"
        android:tint="@color/gray_800"
        app:srcCompat="@drawable/topo"
        android:visibility="invisible"
         />

    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/sat_img_style_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintEnd_toEndOf="@+id/topo_style_button"
        app:layout_constraintTop_toBottomOf="@+id/topo_style_button"
        android:layout_marginTop="@dimen/margin_standard"
        android:backgroundTint="@color/gray_200"
        android:clickable="true"
        android:focusable="true"
        android:tint="@color/gray_800"
        app:srcCompat="@drawable/satellite_image"
        android:contentDescription="@null"
        android:visibility="invisible"
        />
</androidx.constraintlayout.widget.ConstraintLayout>